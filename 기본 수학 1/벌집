#include <stdio.h>

int main() {
	int N;
	int num = 1;
	int loop = 0;
	int endNum = 1;

	scanf("%d", &N);

	if (N == 1) {
		printf("1");
		return 0;
	}

	for (int i = 1;; i++) {
		loop++;
		endNum += i * 6;
		if (endNum >= N) {
			printf("%d", loop + 1);
			break;
		}
	}

	return 0;
}
