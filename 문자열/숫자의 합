#include <stdio.h>

int main() {
	int size;

	scanf("%d", &size);

	char* arr = (char*)malloc(sizeof(char)* size);

	scanf("%s", arr);

	int sum = 0;

	for (int i = 0; i < size; i++) {
		sum += arr[i] - '0';
	}

	printf("%d\n", sum);

	return 0;
}
